<!DOCTYPE html>
<html>
    <body>
    	<% if logged_in? %>
			<div class="row">
			    <aside class="col-md-4">
			      <section class="user_info">
			        <%= render 'shared/user_info' %>
			      </section>
			      <%= link_to 'Add Quiz', new_user_quiz_path(current_user), class: "btn btn-primary"%>
			    </aside>
			    <div class="col-md-8">
			      <h3>My Quizzes</h3>
			      <%= render 'shared/feed' %>
			    </div>
		    </div>
		<% else %>
	        <div class="center jumbotron">
	            <h1>Welcome to the Interactive+ !</h1>
	        </div>
	        <p>In order to create a quiz you need to sign up. <%= link_to "Sign up now!", signup_path %></p>
	    <% end %>
    </body>
</html>
